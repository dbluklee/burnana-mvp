# Dockerfile.dev
FROM node:18-alpine

# 개발 도구 설치
RUN apk add --no-cache git curl

WORKDIR /app

# package.json 복사 및 의존성 설치
COPY package*.json ./
RUN npm install

# 포트 노출
EXPOSE 3000

# nodemon으로 개발 서버 실행
CMD ["npm", "run", "dev"]